# 보안 안내

## GitHub에 올라가는 것

- 정적 웹 코드
- 공개 관심종목 분석 결과
- 숫자 4자리 PIN으로 암호화된 포트폴리오·최근 거래 데이터
- 암호화된 누적 실현손익·입출금·시간별 자산 이력
- 암호화 형식에 필요한 salt, IV, 반복 횟수와 마지막 갱신 시각

## Git 저장소와 정적 웹에 올라가지 않는 것

- NHPLUG AppKey와 AppSecret
- 선택 계좌번호 원문
- 포트폴리오 PIN
- 평문 포트폴리오와 거래내역

수동 업데이트만 사용할 때 AppKey, AppSecret, 선택 계좌, 포트폴리오 PIN은 Python `keyring`을 통해 현재 Windows 사용자의 자격 증명 저장소에 보관됩니다. `.env`나 소스 파일에 인증정보를 적지 마세요.

시간당 GitHub Actions 업데이트를 활성화하면 다음 값은 GitHub Actions Secret에도 저장됩니다.

- AppKey와 AppSecret
- 선택 계좌번호 원문
- 포트폴리오 PIN

Secret은 정적 웹이나 일반 로그에 표시되지 않지만, 저장소의 워크플로를 수정할 권한이 있는 계정이 침해되면 악성 워크플로를 통해 유출될 수 있습니다. GitHub 계정에 2단계 인증을 적용하고 저장소 쓰기 권한을 최소화하세요. 증권사 API 키 자체에 주문 권한이 포함될 수 있으므로 이 위험을 받아들이기 어렵다면 GitHub Actions 자동 조회를 사용하지 말고 Windows 작업 스케줄러에서 `publish_update.bat`을 실행하세요.

## 반드시 알아둘 점

Firebase Hosting과 GitHub Pages는 사설 호스팅이 아닙니다. 사이트 코드와 암호문은 누구나 내려받을 수 있다고 가정해야 합니다. 숫자 4자리 PIN은 가능한 조합이 10,000개뿐이므로 공격자가 암호문을 내려받아 모든 조합을 시도할 수 있습니다. 따라서 이 PIN은 가족·동료가 우연히 화면을 여는 것을 막는 간단한 잠금이며, 자산정보의 강한 기밀성을 보장하지 않습니다.

브라우저는 Web Crypto API로 데이터를 복호화합니다. 입력한 PIN은 서버에 전송하거나 `localStorage`, 쿠키에 저장하지 않습니다. **잠그기**를 누르거나 페이지를 다시 열면 재입력해야 합니다.

GitHub Actions 자동 조회에서는 PIN을 이용해 기존 이력을 임시 실행 환경의 메모리에서 복호화하고, 새 데이터를 병합한 뒤 다시 암호화합니다. 평문 JSON 파일을 커밋하지 않으며 주문 API는 호출하지 않습니다.

## PIN 변경과 Git 기록

`change_web_password.bat`은 현재 데이터를 새 PIN으로 다시 암호화하지만, Git 커밋 기록에는 과거 암호문이 남습니다. 과거 PIN이 노출된 경우 단순 변경만으로 예전 커밋을 보호할 수 없습니다. 이때는 새 저장소를 만들고 새 인증정보로 현재 파일만 게시해야 과거 파일 접근을 차단할 수 있습니다.

PIN을 잊어버리면 GitHub의 암호문만으로는 정상 복구할 수 없습니다. 로컬 Windows 자격 증명과 원본 증권사 데이터가 남아 있을 때 새 PIN으로 다시 업데이트하세요.

## 사용상 주의

- 공용 PC에서는 웹을 열지 마세요.
- 저장소에 평문 JSON, 화면 캡처, API 키를 직접 커밋하지 마세요.
- GitHub의 Dependabot 및 보안 알림을 켜 두는 것을 권장합니다.
- 웹에서 주문을 실행하지 않습니다. 조회 API만 사용합니다.
- 계정 침해가 의심되면 NHPLUG AppSecret을 증권사에서 폐기·재발급하고 GitHub 로그인 세션도 확인하세요.
