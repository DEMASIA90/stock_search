# 눌림목 / 돌파 스캐닝 로직 — V3 최종 명세

V2 대비 변경 요약: **회귀 5건 수정 + 논리충돌 5건 수정 + 지표 스트림 5종 삭제**

---

# 0. 삭제 항목과 근거

## 0-1. 완전 삭제한 지표 스트림

| 지표 | 사용처 (V2) | 삭제 근거 |
|---|---|---|
| **MACD** | 눌림목 G6 | `MA20>MA50 ∧ C>MA50` 이면 `EMA12>EMA26` 이 거의 자동. 게다가 깊은 눌림에서만 선택적으로 0 이하로 떨어져 **S1(r=0.45 최대)이 최고점을 주는 종목을 골라서 탈락**시킴. 중복 + 역방향 작동 |
| **RSI** | 눌림목 G5, 돌파 G4 | 눌림목: RSI는 최근 되돌림의 단조 변환 ≒ `r`의 열화 버전. `r`이 직접·정확하게 측정하므로 정보 추가 0<br>돌파: `C>20일 신고가 ∧ C>MA50>MA120` 조건에서 `RSI≥55` 통과율 90%+ → 게이트로서 무의미 |
| **PSAR** | 눌림목 S7 (8%) | 가격 극값의 결정론적 함수. S4(캔들 위치)·S1(r)과 중복. 파라미터 2개(AF, AF_max) 추가 부담 |
| **Heikin-Ashi** | 눌림목 S6 (10%) | 별도 캔들 스트림 전체가 필요한데, 실제 정보는 "오늘 종가가 강하게 마감했는가" → G9 + S4(raw CloseLocation)로 동일 정보 획득 |
| **Bollinger Band** | 돌파 S3 (VCP) | ATR14는 손절 계산에 이미 필요. `ATR14/C`의 250일 백분위가 `BBWidth`의 250일 백분위와 사실상 동일 정보 → 지표 하나로 통합 |

**최종 필요 지표: MA20, MA50, MA120, ATR14, OBV, OHLCV.** (6종)

## 0-2. 삭제한 게이트

| 게이트 (V2) | 삭제 근거 |
|---|---|
| 눌림목 G4 `C ≥ MA50×0.98` | **G1의 `C > MA50`에 의해 항상 참 → 죽은 조건.** V3에서는 반대로 G1의 가격조건을 없애고 이쪽을 완화판(×0.97)으로 남김 |
| 눌림목 G3 `\|C-MA20\|/MA20 ≤ 0.07` | 상단은 `r ≥ 0.20`이 이미 보장. 하단은 깊은 눌림(r→0.70)을 배제해 S1과 충돌 |
| 눌림목 G6 MACD | 0-1 참조 |
| 눌림목 G5 RSI | 0-1 참조 |
| 돌파 G4 RSI | 0-1 참조 |
| 돌파 G5의 `MA50 > MA120` 부분 | 골든크로스 완료를 요구 → **장기 베이스 돌파(최상급 셋업)를 배제.** `C > MA50 ∧ C > MA120`만 유지하고 `MA50/MA120-1`은 진단 컬럼으로 저장 |

## 0-3. 삭제한 점수 항목

| 항목 (V2) | 삭제 근거 |
|---|---|
| 돌파 S1 돌파레벨 티어 (20%) | **S4(근접 매물대)의 열화 버전.** 52주 신고가 → 위쪽 매물 0 → S4=100. 20일 돌파만 → 매물 존재 → S4 낮음. S4는 여기에 "물량이 얼마나" + "시간가중"까지 담으므로 정보 상위집합. `BreakoutTier`는 진단 컬럼으로만 유지 |
| 눌림목 S6 HA / S7 PSAR | 0-1 참조 |

## 0-4. 삭제한 구조

| 구조 (V2) | 삭제 근거 |
|---|---|
| `RiskNormalizedScore = Percentile / sqrt(Risk×100)` | Percentile 60·Risk 3%가 Percentile 95·Risk 12%를 이기는 역전 발생. **선정(Score)과 사이징(등리스크)을 분리**하면 이 항목 자체가 불필요 |
| 두 전략 통합 랭킹 | `같은 Percentile = 같은 기대수익`은 검증 대상이지 전제가 아님. V3는 리스트 분리 + 고정 배분 |
| 당일 후보군 내 Percentile | 표본 크기에 따라 원점수 순서가 역전. 250일 누적 분포 기준으로 교체 |

---

# 1. 공통

## 1-1. 데이터 원칙

```text
가격 지표     → 수정 OHLC (Corporate Action 반영)
거래량 지표   → Split-adjusted Volume
유동성 필터   → 거래소 원본 거래대금
매물대 계산   → 수정가격 × 수정거래량 (정합성 유지)

금지: 수정주가 × 원거래량으로 과거 거래대금 재계산
```

## 1-2. 유니버스 하드필터

```text
U1  상장 후 280거래일 이상
    ※ V2의 250일은 부족
       S3 VCP: ATRP[t-250]가 필요 → ATR14 워밍업 14일 추가
       → 최소 264, 여유 포함 280

U2  최근 20일 평균 거래대금 ≥ 10억원

U3  관리종목 / 투자주의·경고·위험 / 정리매매 제외

U4  우선주 / SPAC / ETF / ETN / REITs 제외

U5  당일 상한가 마감 제외

U6  당일 거래정지 제외
```

## 1-3. 스캔 시점

```text
t = 마지막 확정 일봉
t 이후 데이터는 선정·Score·Gate 판정에 사용 금지
실전: t 장 마감 후 스캔 → t+1 시가 진입
```

## 1-4. 시장국면 배율 M — **순위가 아니라 임계값에 적용**

```text
Idx_C[t] > Idx_MA20[t] AND Idx_MA20[t] > Idx_MA60[t]  → M = 1.00
Idx_C[t] > Idx_MA20[t]                                 → M = 0.85
그 외                                                   → M = 0.60

Score = RawScore × M
```

**진입 판정 (V2에서 M이 무력화된 지점의 수정)**

```text
E1  절대 임계값
    Score ≥ 50 인 종목만 진입 대상
    → M=0.60 이면 Raw 83 이상이어야 통과 → 약세장 자동 축소

E2  당일 신규 진입 수 상한
    MaxEntries[t] = round(5 × M)
    → M=1.00: 5종목 / M=0.85: 4종목 / M=0.60: 3종목

E3  Percentile 은 진입 판정에 사용하지 않음 (비교·기록용)
```

**근거**: Percentile은 순위 기반 변환이므로 양의 상수배 M에 불변.
`percentile_rank(Raw × M) = percentile_rank(Raw)` → V2에서 M은 최종 순위에 영향이 없었음.

## 1-5. 참조 백분위 (V2의 당일 후보군 기준 → 교체)

```text
StrategyPercentile =
percentile_rank(
    Score[t],
    해당 전략의 최근 250거래일 누적 Score 분포
)

→ 표본 크기 무관, 날짜 간 비교 가능, M의 효과 보존
```

---

# 2. 눌림목 매매

## 2-1. 스윙 구조

```text
i_H = argmax(High[t-20 ... t-3])
H_sw = High[i_H]
PullbackDays = t - i_H                          ∈ [3, 20]

i_L = i_H 직전에 C[i] < MA20[i] 였던 마지막 날 + 1
      (없으면 i_H - 40)
      ※ 임펄스 레그의 시작점 = MA20 위로 올라선 시점

L_base = min(Low[i_L ... i_H])
SwingRise = (H_sw - L_base) / L_base
r = (H_sw - C[t]) / (H_sw - L_base)
```

**V2 대비 변경**: `L_base = min(Low[i_H-60 ... i_H])` → 임펄스 레그 기준.
60일 룩백은 추세가 길수록 분모를 키워 `r`을 체계적으로 과소평가했음.
(60일 상승 100→200 후 185로 조정 시 r=0.15 → G11 Fail. 실제로는 유효한 눌림)

**탈락 조건**

```text
H_sw ≤ L_base                    → 제외
i_H - i_L < 5                    → 레그 과소 → 제외
H_sw < max(High[i_H-60 ... i_H-1]) → 제외 (G5)
    ※ 이 조건이 "진짜 스윙고점이 탐색창 밖(t-20 이전)에 있는 경우"도
      자동으로 걸러냄 → 별도 가드 불필요
```

## 2-2. 게이트 (전부 AND) — 12개 → 10개

```text
G1  MA20[t] > MA50[t]
    ※ V2의 C[t] > MA50[t] 는 제거 (G2로 완화 이관)

G2  C[t] ≥ MA50[t] × 0.93
    ※ G6 과 상호 구속 관계. 선형 상승 40일 + 조정 12일 근사로 계산하면

        MA50 ≈ [38×(L_base + 0.51Δ) + 12×(H_sw + C)/2] / 50,  Δ = H_sw - L_base

        r = 0.566 → C/MA50 ≈ 0.970
        r = 0.70  → C/MA50 ≈ 0.942

      즉 TOL = 0.97 이면 r 은 실질적으로 0.57 을 넘지 못하고,
      G6 의 상한 0.70 과 S1 의 정의역이 무의미해진다.
      → r 이 구속조건이 되도록 TOL 을 0.93 으로 완화 (r=0.65 에서 C/MA50 ≈ 0.951)
      G2 는 이제 "MA50 이 과거 급등 때문에 비정상적으로 높은" 병리적 케이스만 차단

G3  MA50[t] > MA50[t-20]
    ※ V2의 MA20[t] > MA20[t-10] 에서 교체
       9~12일 × 45% 되돌림이면 MA20 10일 기울기는 거의 확실히 음수
       → G2(V2)가 S1·S5의 만점 구간을 통째로 탈락시키는 자기모순이었음
       MA50은 조정 12일에 잘 꺾이지 않음

G4  SwingRise ≥ 0.15

G5  H_sw ≥ max(High[i_H-60 ... i_H-1])

G6  0.20 ≤ r ≤ 0.65
    ※ 상한 0.70 → 0.65 (G2 와 정합. 위 유도 참조)
       추세 훼손 경계이기도 하며, S1 이 r=0.65 에 주는 점수는 33점에 불과

G7  DryVol ≤ 0.85
    DryVol = mean(V[i_H+1 ... t-1]) / mean(V[i_L ... i_H])
    ※ V2의 고정 윈도우(mean(V[t-5:t-1])/mean(V[t-20:t-6]))에서 교체
       PullbackDays=3  → 분자에 상승 마지막 대량거래 포함 → Fail
       PullbackDays=15 → 분모가 전부 조정구간 → 비율 1.0 → Fail
       즉 S5가 만점 주는 9~12일 구간을 G7이 탈락시키던 구조
       레그 기준은 기간 길이에 무관한 무차원량

G8  RecoveryVol ≥ 1.20
    RecoveryVol = V[t] / mean(V[i_H+1 ... t-1])

G9  CloseLocation ≥ 0.40
    CloseLocation = (C[t] - L[t]) / (H[t] - L[t])
    H[t] == L[t] → Fail

G10 EstimatedRisk ≤ 0.12
    ※ V2는 clip(risk, 0.03, 0.12) 로 잘라서 실제 손실폭을 은폐했음
       상한은 클립이 아니라 게이트로 처리
```

## 2-3. 점수 (7항목, 합 100%)

### S1 되돌림 깊이 — 24%

```text
S1 = 100 × exp[ -(r - 0.45)² / (2 × 0.15²) ]

r = 0.20 →  25점
r = 0.38 →  87점   (38.2% 되돌림)
r = 0.45 → 100점
r = 0.50 →  95점   (50% 되돌림)
r = 0.62 →  61점   (61.8% 되돌림)
r = 0.65 →  33점
```

### S2 거래량 마름 — 18%

```text
d = DryVol   (G7과 동일 정의, 레그 기준)

S2 = 100 × clip( (0.95 - d) / 0.35, 0, 1 )

d ≥ 0.95 → 0     (게이트 0.85 통과 시 최소 28.6점)
d ≤ 0.60 → 100
```

### S3 당일 거래량 회복 — 10%

```text
p = RecoveryVol

S3 = 100 × clip( (p - 1.10) / 0.90, 0, 1 )

p ≤ 1.10 → 0     (게이트 1.20 통과 시 최소 11.1점)
p ≥ 2.00 → 100
```

### S4 반전 캔들 강도 — 8%

```text
a = CloseLocation[t]

S4 = 100 × clip( (a - 0.40) / 0.50, 0, 1 )

a = 0.40 → 0
a ≥ 0.90 → 100

※ V2의 HA 4단계 분류를 raw 캔들 연속함수로 대체
  (HA 스트림 제거, 동일 정보)
```

### S5 추세 강도 — 12%

```text
g50 = (MA50[t] - MA50[t-10]) / (MA50[t-10] × 10)

S5 = 100 × clip( g50 / 0.0025, 0, 1 )

MA50 일평균 상승률 0.25% 이상 → 100점
```

### S6 눌림 기간 — 8%

```text
days = PullbackDays

S6 = 100 × exp[ -(days - 9)² / (2 × 5²) ]

days =  3 → 48.7
days =  9 → 100
days = 20 →  8.9
```

### S7 상대강도 — 20%  ★ V3 신설

```text
Ret126 = C[t] / C[t-126] - 1
RS = percentile_rank(Ret126, 당일 U필터 통과 전체 유니버스) × 100

S7 = clip( (RS - 40) / 0.6, 0, 100 )

RS = 40  → 0
RS = 70  → 50
RS = 100 → 100
```

**근거**: 돌파 모델에는 RS가 있었으나 눌림목에는 없었음.
주도주의 눌림과 소외주의 눌림은 전혀 다른 사건이며, 눌림목 모델에서 가장 큰 누락이었음.

**알려진 편향**: Ret126이 진행 중인 눌림(-10% 수준)만큼 깎임.
모든 후보에 동일하게 작용하므로 순위 왜곡은 제한적.
진단 컬럼 `RS_swing`(= `H_sw / C[t-126] - 1` 기준 백분위)을 병행 저장해 사후 비교.

```text
RawScore_PB =
  0.24×S1 + 0.18×S2 + 0.10×S3 + 0.08×S4
+ 0.12×S5 + 0.08×S6 + 0.20×S7

Score_PB = RawScore_PB × M
```

## 2-4. 손절

```text
Stop_PB = max(
    min(Low[i_H+1 ... t]) - 0.5 × ATR14[t],
    C[t] × 0.88
)
```

**V2 대비**: `min(Low[t-4 ... t])` → 조정 레그 실제 저점.
PullbackDays=12이고 저점이 7일 전이면 V2 손절은 실제 지지선 **위**에 걸려 휩쏘.

---

# 3. 돌파 매매

## 3-1. 게이트 (전부 AND) — 7개 → 7개 (구성 변경)

```text
BreakoutLevel20 = max(High[t-20 ... t-1])

G1  C[t] > BreakoutLevel20 × 1.005

G2  m_vol = V[t] / mean(V[t-60 ... t-1]) ≥ 1.50
    ※ V2의 거래대금(TV) 배수에서 순수 거래량 배수로 교체
       TV = 가격 × 거래량 이므로 60일간 40% 오른 종목은
       거래량 1.25배만으로 TV 1.5배를 통과 → 이미 오른 종목에 편향
       S7(RS)·매물대와 같은 방향으로 편향이 누적됨
       거래대금은 유동성 필터(U2)에만 사용
       m_tv 는 진단 컬럼으로 병행 저장

G3  CloseLocation ≥ 0.65
    H[t] == L[t] → Fail

G4  C[t] > MA50[t]  AND  C[t] > MA120[t]
    ※ V2의 MA50 > MA120 요구는 제거 (장기 베이스 돌파 배제 방지)
       MA50/MA120-1 은 진단 컬럼

G5  C[t] / MA20[t] ≤ 1.30

G6  C[t] / C[t-1] - 1 ≤ 0.15

G7  EstimatedRisk ≤ 0.12
```

## 3-2. 매물대 계산 — 로그 Bin

```text
룩백  = [t-120 ... t-1]     ※ 당일 t 제외
        (V2의 200일 → 120일. 반감기 45일이면 200일 전 가중치 0.046)

Bin   = C[t] × 1.01^k,  k = -25 ... +25
        → 1% 폭 로그 Bin 50개, 대역 ±28%
        Bin 중앙가 P_j = sqrt(edge_j × edge_{j+1})

※ V2는 200일 전체 범위를 20등분했는데 분석 대역만 ±20%로 좁혀서
  Overhead 대역에 Bin이 1~2개만 들어가는 경우가 발생
  (범위 5,000~25,000 → Bin폭 1,000원 → C=24,000이면 Overhead Bin 1개)
  → SupplyRatio가 1/6 단위로 양자화되어 연속 점수 의미 소멸

일별 거래대금
TV_i = V_i × (H_i + L_i + C_i) / 3     (원본 거래대금 있으면 그것 사용)

Bin 배분
[L_i, H_i] 와 각 Bin의 겹치는 길이 비례로 TV_i 배분
분모는 (H_i - L_i) 전체       ※ 격자 밖 부분은 버림
H_i == L_i → TypicalPrice 가 속한 Bin에 전량

시간가중
w_i = 0.5 ^ ((t - i) / 45)             반감기 45거래일

BinValue[j] = Σ (배분 TV × w_i)
```

```text
Overhead = Σ BinValue[j] ,  C[t] < P_j ≤ 1.20 × C[t]
Nearby   = Σ BinValue[j] ,  0.80 × C[t] ≤ P_j ≤ 1.20 × C[t]

SupplyRatio = Overhead / Nearby        (Nearby = 0 → 0)
```

## 3-3. 점수 (6항목, 합 100%)

### S1 근접 매물대 — 25%  ★ V2의 S1(돌파레벨 티어) 흡수

```text
S1 = 100 × clip( 1 - SupplyRatio / 0.50, 0, 1 )

SupplyRatio = 0    → 100   (위쪽 매물 없음 = 신고가권)
SupplyRatio ≥ 0.50 → 0     (근접 매물의 절반 이상이 머리 위)
```

**V2의 돌파레벨 티어 삭제 근거**: 티어는 "위쪽에 더 높은 가격이 있었나"의 이진 근사.
매물대는 여기에 **물량 크기 + 시간가중**을 더한 상위집합.
두 항목의 상관은 0.8 이상으로 추정되며 V2에서 합계 35%가 사실상 한 요인에 배정돼 있었음.

### S2 거래량 확장 — 20%

```text
m = m_vol

S2 = 100 × clip( log(m / 1.5) / log(4.0 / 1.5), 0, 1 )

m = 1.5 → 0
m = 2.0 → 29
m = 3.0 → 71
m = 4.0 → 100

※ 선형 → 로그. 1.5~2.5 구간의 분해능을 높이고
  고배수 구간의 포화를 완만하게
```

### S3 변동성 수축 VCP — 20%

```text
ATRP[i] = ATR14[i] / C[i]

v = mean( ATRP[t-5 ... t-1] )          5일 평활 (V2의 단일일 스냅샷 교체)
q = percentile_rank( v, ATRP[t-250 ... t-1] )   ∈ [0, 1]

S3 = 100 × clip( (0.45 - q) / 0.40, 0, 1 )

q ≤ 0.05 → 100
q ≥ 0.45 → 0

※ BBWidth → ATR 기반으로 교체 (지표 스트림 1종 제거,
  ATR14는 손절 계산에 이미 필요)
  BBWidth 백분위는 진단 컬럼으로 병행 저장 후 IC 비교
```

### S4 OBV 축적 다이버전스 — 15%  ★ 정의 전면 교체

```text
obv_pct   = percentile_rank( OBV[t-1], OBV[t-60 ... t-1] )
price_pct = percentile_rank( C[t-1],   C[t-60 ... t-1]   )

Div = obv_pct - price_pct              ∈ [-1, 1]

S4 = 100 × clip( (Div + 0.10) / 0.40, 0, 1 )

Div ≤ -0.10 → 0     가격 대비 OBV 부진 (분산)
Div =  0.10 → 50
Div ≥ +0.30 → 100   베이스에서 OBV 선행 (축적)
```

**V2 정의가 붕괴한 이유**

```text
G1  C[t] > max(High[t-20:t-1]) ≥ High[t-1] ≥ C[t-1]
    → C[t] > C[t-1] 이 구조적으로 보장

G2  V[t] ≥ 1.5 × 60일 평균

→ OBV[t] = OBV[t-1] + V[t] 가 대폭 증가
→ 상승추세 종목에서 OBVHighIdx = t 가 거의 항상 성립
→ lead = PriceHighIdx - t ≤ 0

결과: 60일 신고가면 lead=0 → 85점 / 아니면 lead<0 → 20점
      lead=1~3 → 100 구간은 도달 불가
      12% 가중치가 "60일 신고가 여부" = S1의 복제본에 배정돼 있었음
```

V3는 **돌파일 t를 제외한 t-1 시점**에서 측정 → "돌파 전 베이스에서 물량이 축적됐는가"라는 원래 의도를 정확히 구현하며 연속·비퇴화.

### S5 상대강도 — 15%

```text
Ret63 = C[t] / C[t-63] - 1
RS = percentile_rank(Ret63, 당일 U필터 통과 전체 유니버스) × 100

S5 = clip( (RS - 50) × 2, 0, 100 )
```

### S6 돌파 캔들 강도 — 5%

```text
k = CloseLocation[t]

S6 = 100 × clip( (k - 0.65) / 0.30, 0, 1 )
```

```text
RawScore_BO =
  0.25×S1 + 0.20×S2 + 0.20×S3
+ 0.15×S4 + 0.15×S5 + 0.05×S6

Score_BO = RawScore_BO × M
```

## 3-4. 손절

```text
Stop_BO = BreakoutLevel20 - 1.0 × ATR14[t]
```

---

# 4. 리스크 · 사이징 · 포트폴리오

## 4-1. 리스크

```text
스캔 시점 (t+1 진입가 미지)
EstimatedRisk = (C[t] - Stop) / C[t]
StopATR       = (C[t] - Stop) / ATR14[t]     ← 손절 계수 사후 최적화용

백테스트
Entry = Open[t+1]
ActualRisk = (Entry - Stop) / Entry
Gap% = Open[t+1] / C[t] - 1                  ← 필터가 아니라 분석 변수
```

## 4-2. 사이징 — 선정과 분리 (V2의 RiskNormalizedScore 대체)

```text
RiskPerTrade = 계좌 × 0.005 × M

주식수 = floor( min(
    RiskPerTrade / (Entry - Stop),        등리스크
    계좌 × 0.20 / Entry                    비중 상한
) )

→ 손절폭이 넓은 종목은 자동으로 비중 축소
→ 손절폭이 극단적으로 좁은 종목(risk 3% 미만)은 비중 상한이 차단
→ 선정 로직에 Risk를 섞을 이유 소멸

※ V2의 RiskAdj 하한 클립(0.03)은 RiskNormalizedScore 전용 장치였으므로
  해당 항목 삭제와 함께 제거. 대신 비중 상한이 같은 역할을 수행
```

## 4-3. 실행 가능성 필터 (t+1)

```text
X1  t+1 거래정지                → Skip
X2  Open[t+1]/C[t] - 1 ≥ 0.29   → 시가 상한가, 체결 불가 → Skip
X3  Entry ≤ Stop                → Skip
```

## 4-4. 포트폴리오 제약

```text
P1  동일 섹터 동시 보유 ≤ 2종목
P2  동일 종목 재신호 쿨다운 5거래일
P3  한 종목이 양쪽 게이트 동시 통과 → Score 높은 쪽 1개만 채택
P4  동시 보유 종목 수 ≤ 8
P5  당일 신규 진입 ≤ round(5 × M)
P6  전략별 고정 배분 (눌림목 : 돌파 = 1 : 1)
    ※ 통합 랭킹은 §6 ①이 Score→기대수익 매핑을 산출한 뒤 도입
```

---

# 5. 출력 컬럼

## 공통

```text
Date / 종목코드 / 종목명 / 시장 / 기법
RawScore / M / Score / StrategyPercentile
S1 ... S7 (항목별 개별 컬럼)
ATR14 / StopPrice / EstimatedRisk% / StopATR
```

## 눌림목 추가

```text
i_H / i_L / H_sw / L_base / SwingRise% / r / PullbackDays
DryVol / RecoveryVol / CloseLocation
MA20 / MA50 / MA50_slope
RS_percentile / RS_swing        (진단)
```

## 돌파 추가

```text
BreakoutLevel20 / Breakout% / BreakoutTier      (진단)
m_vol / m_tv                                     (진단: TV 배수 비교용)
ATRP_percentile / BBWidth_percentile             (진단: VCP 방식 비교용)
SupplyRatio / Overhead / Nearby
OBV_Div / obv_pct / price_pct
RS_percentile / CloseLocation
MA50_over_MA120                                  (진단)
```

## 백테스트 전용

```text
EntryDate / EntryPrice / Gap% / ActualRisk%
FwdRet5 / FwdRet10 / FwdRet20                    절대수익
ExRet5  / ExRet10  / ExRet20                     지수 초과수익  ← 검증 기준
MAE20 = min(Low[t+1...t+20]) / Entry - 1
MFE20 = max(High[t+1...t+20]) / Entry - 1
TradeRet                                          손절 반영 실현손익
```

---

# 6. 검증 절차

```text
① Decile Test
   기법별 Score 1~10분위 × ExRet 5/10/20D
   ※ 절대수익이 아니라 지수 초과수익 기준
      강세장 구간에서는 모든 Decile이 양수로 나와 오독됨

② 대조군 비교
   Gate 통과 전체 후보 동일가중 성과 = baseline
   Score Decile이 baseline을 이기는지 확인
   → Gate의 효과와 Score의 효과를 분리

③ 항목별 IC
   각 S_i vs ExRet, 월별 Spearman
   평균 IC / 표준편차 / ICIR / 양의 IC 월 비율 / 국면별 IC

④ 다중검정 보정
   눌림목 7 + 돌파 6 = 13항목 × 3 horizon = 39회 검정
   → Benjamini-Hochberg 적용
   → 또는 "ICIR > 0.5 가 12개월 이상 지속" 을 채택 기준으로

⑤ Gate 개별 기여도
   각 Gate를 하나씩 제거한 버전으로 재실행
   성과가 유지되면 삭제 (자유도 절약)

⑥ 상관행렬
   S1~S7 간 Spearman 상관
   ρ > 0.7 인 쌍은 병합 또는 한쪽 삭제
   ※ 특히 돌파 S1(매물대) - S5(RS) 를 우선 확인

⑦ 손절 계수 최적화
   StopATR vs 성과 관계 확인
   MAE20 분포로 "몇 %가 -1.5 ATR을 건드리는가" 측정
   → 0.5 / 1.0 ATR 계수를 데이터로 결정

⑧ Walk-Forward
   Train 2~3년 / Validation 6개월 / Test 6개월, 구간 이동
   또는 Expanding Window

⑨ Purging / Embargo
   20일 forward return은 인접 신호끼리 결과기간 중첩
   → 가중치 최적화 단계에서는 필수

⑩ 거래비용
   매수·매도 수수료 / 세금 / 슬리피지
   Gross / Net 모두 저장

⑪ Survivorship Bias
   각 날짜별 당시 실제 상장 유니버스 사용
   상폐예정·관리·거래정지 상태도 그 시점 기준 적용

⑫ 게이트 통과 수 시계열
   0종목 발생률 목표 < 20%
   ※ 이를 맞추려고 Gate를 임의 완화하지 않음
```

---

# 7. 인덱싱 규약

```text
문서 표기  = 양끝 포함
Python     = 오른쪽 끝 미포함

V[t-5 ... t-1]        → .iloc[t-5 : t]        5개
V[t-20 ... t-6]       → .iloc[t-20 : t-5]     15개
Low[t-4 ... t]        → .iloc[t-4 : t+1]      5개
High[t-60 ... t]      → .iloc[t-60 : t+1]     61개
Low[i_L ... i_H]      → .iloc[i_L : i_H+1]    (i_H-i_L+1)개
High[t-20 ... t-3]    → .iloc[t-20 : t-2]     18개
```

**음수 인덱스 가드 필수**

```text
pandas .iloc / numpy 는 음수 시작값을 배열 뒤에서부터 해석
→ 가드가 없으면 조용히 잘못된 구간을 읽고 정상 결과처럼 반환

if t < 280:        후보 제외
if i_H - 60 < 0:   후보 제외
```

---

# 8. V2 → V3 변경 대조표

| # | 항목 | V2 | V3 |
|---|---|---|---|
| 1 | 시장국면 M | Score에 곱 → Percentile이 상쇄 | 절대 임계값 + 진입수 상한 |
| 2 | 돌파 OBV | argmax 기반, 85/20 이진 붕괴 | t-1 기준 백분위 다이버전스 |
| 3 | Risk 상한 | clip(0.12) — 실제 손실 은폐 | 게이트로 처리 |
| 4 | 매물대 Bin | 200일 등폭 20개 | C[t] 기준 1% 로그 Bin 50개 |
| 5 | Percentile | 당일 후보군 | 250일 누적 분포 |
| 6 | 눌림목 추세 게이트 | MA20 10일 기울기 | MA50 20일 기울기 |
| 7 | DryVol | 고정 윈도우 | 임펄스/조정 레그 기준 |
| 8 | L_base | i_H 기준 60일 최저 | 임펄스 레그 시작점 |
| 9 | 돌파 수급 | 거래대금 배수 | 순수 거래량 배수 |
| 10 | 돌파 S1 | 레벨 티어 20% | 삭제 → 매물대 25%로 흡수 |
| 11 | 눌림목 S6/S7 | HA 10% + PSAR 8% | 삭제 → raw 캔들 8% |
| 12 | 눌림목 RS | 없음 | 신설 20% |
| 13 | VCP | BBWidth 단일일 | ATR 5일 평활 |
| 14 | RiskNormalizedScore | Percentile/sqrt(Risk) | 삭제 → 등리스크 사이징 |
| 15 | 통합 랭킹 | 운영식 | 보류, 고정 배분 |
| 16 | 검증 수익률 | 절대수익 | 지수 초과수익 |
| 17 | U1 | 250일 | 280일 |
| 18 | 지표 스트림 | 9종 | 6종 |

---

# 9. 부록 — 참조 구현 검증 결과

`scanner_v3.py` 를 합성 데이터로 실행해 확인한 내용.

## 9-1. 돌파 S1 삭제 근거 (실측)

돌파 게이트 통과 표본 474건에 대한 Spearman 상관:

```text
ρ( S1 근접매물대,  삭제한 돌파레벨 티어 ) = +0.878
```

→ 두 항목이 사실상 같은 것을 측정. V2에서 합계 35%가 한 요인에 배정돼 있었음이 확인됨.

**V3 돌파 점수 항목 간 상관행렬**

```text
             S1매물대   S2거래량    S3 VCP    S4 OBV    S6캔들
S1 매물대       1.00     -0.01     -0.12     -0.04      0.13
S2 거래량      -0.01      1.00      0.07     -0.03     -0.01
S3 VCP        -0.12      0.07      1.00      0.04     -0.03
S4 OBV        -0.04     -0.03      0.04      1.00      0.00
S6 캔들        0.13     -0.01     -0.03      0.00      1.00
```

최대 |ρ| = 0.13 → **거의 직교**. V2의 요인 집중(57%가 한 요인) 문제 해소 확인.
S5(RS)는 횡단면 항목이라 단일종목 시뮬레이션에서는 제외됨 → 실데이터 백테스트에서 재확인 필요.

## 9-2. 눌림목 게이트 병목 (합성 눌림 패턴 300표본)

```text
PASS                166  55%
G6 r 범위             85  28%   ← 주 구속조건
G2 MA50 이탈          30  10%
G1 MA20>MA50         13   4%
swing/G5              4   1%
G4 SwingRise          1   0%
G8 RecoveryVol        1   0%
G3 MA50 기울기          0   0%
G7 DryVol             0   0%    ← 아래 주의사항 참조
G9 CloseLoc           0   0%
```

**해석**

```text
1. 병목이 r(눌림 깊이)에 집중 = 의도한 설계
   V2는 PSAR·MACD 같은 부차 지표가 병목이었음

2. 조정폭 × 조정일수 격자 통과율이 대각 밴드 형태로 나타남
   (얕고 짧음 → r 미달 / 깊고 김 → r 초과)
   → r 하나가 깊이·기간을 통합 측정하고 있음이 확인됨

3. G3(MA50 기울기)·G7(DryVol)·G9(CloseLoc) 는 0% 탈락
   → 합성 데이터가 해당 축을 충분히 흔들지 않았기 때문 (테스트 한계)
   → 실데이터에서 §6 ⑤(Gate 개별 기여도)로 재검증 필요.
     기여도가 0이면 삭제 대상
```

## 9-3. V2 OBV 정의 붕괴 실측

```text
눌림목 표본: argmax(OBV)=45/60, argmax(High)=45/60 → lead = 0
돌파   표본: argmax(OBV)=60/60, argmax(High)=60/60 → lead = 0
```

→ OBV argmax 가 항상 마지막 시점에 고정되어 `lead > 0` 이 발생하지 않음.
V2 점수표의 `lead = 1~3 → 100` 은 도달 불가 구간이었음이 실측으로 확인.

## 9-4. 시장국면 M 작동 확인

RawScore = [85, 70, 55, 45] 인 후보군에 대해:

```text
M = 1.00 → Score = [85.0, 70.0, 55.0, 45.0]  진입가능 3건  MaxEntries 5
M = 0.85 → Score = [72.2, 59.5, 46.8, 38.2]  진입가능 2건  MaxEntries 4
M = 0.60 → Score = [51.0, 42.0, 33.0, 27.0]  진입가능 1건  MaxEntries 3
```

→ 절대 임계값 방식에서 M이 실제로 후보 수를 축소.
V2의 Percentile 방식에서는 순위 불변으로 완전히 상쇄되었음.

## 9-5. 거래대금 배수 편향 실측

동일 돌파 표본에서:

```text
m_vol (순수 거래량 배수) = 4.50
m_tv  (거래대금 배수)    = 5.01     → 11% 과대
```

가격이 오를수록 격차가 벌어지므로, 상승 폭이 큰 종목일수록 G2를 더 쉽게 통과.
V3는 G2/S2에 `m_vol` 사용, `m_tv` 는 진단 컬럼으로만 저장.
